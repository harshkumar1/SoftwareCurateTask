{
  "name": "jfrog-pipelines-task-curate-artifact",
  "version": "0.0.1",
  "description": "Task to cache and copy and artifact from a remote",
  "main": "src/index.ts",
  "scripts": {
    "dist": "ncc build ./src/index.ts -o dist",
    "test": "jest ./src",
    "test:coverage": "jest ./src --coverage",
    "test:report": "jest ./src --coverage --coverageDirectory='coverage'",
    "prettier": "prettier -c ./src",
    "prettier:write": "prettier -w ./src"
  },
  "license": "ISC",
  "dependencies": {
    "axios": "1.1.2",
    "jfrog-pipelines-tasks": "0.0.12"
  },
  "devDependencies": {
    "@types/jest": "^29.2.3",
    "@types/node": "^18.11.9",
    "@vercel/ncc": "^0.34.0",
    "jest": "^28.1.3",
    "mocha": "^10.1.0",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.8",
    "typescript": "^4.8.4"
  }
}
